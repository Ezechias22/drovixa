import Link from 'next/link'; import type { ContentCardData } from '@/features/catalog/types'; import { PlayIcon } from './icons';
export function Hero({ items }: { items: ContentCardData[] }) { const item = items[0]; if (!item) return null; return <section className="relative min-h-[620px] overflow-hidden">{item.backdrop_url && <img alt="" src={item.backdrop_url} className="absolute inset-0 h-full w-full object-cover" />}<div className="absolute inset-0 bg-[linear-gradient(90deg,#08090bf2_5%,#08090b9e_55%,transparent),linear-gradient(0deg,#08090b,transparent_60%)]" /><div className="relative mx-auto flex min-h-[620px] max-w-[1600px] items-end px-5 pb-20 md:items-center md:px-8"><div className="max-w-xl"><p className="text-xs font-black tracking-[.25em] text-[var(--accent)]">{item.premium ? 'DROVIXA ORIGINAL' : 'FEATURED'}</p><h1 className="mt-4 text-5xl font-black tracking-[-.05em] md:text-7xl">{item.title}</h1><p className="mt-5 line-clamp-3 text-lg leading-8 text-white/70">{item.short_description}</p><div className="mt-8 flex gap-3"><Link className="primary-button" href={`/${item.type === 'series' ? 'series' : 'movies'}/${item.slug}`}><PlayIcon size={18} /> Watch now</Link><Link className="secondary-button" href={`/${item.type === 'series' ? 'series' : 'movies'}/${item.slug}`}>More info</Link></div></div></div></section>; }
