import Link from 'next/link'; import type { ContentCardData } from '@/features/catalog/types'; import { PlayIcon } from './icons';
export function Hero({ items }: { items: ContentCardData[] }) { const item = items[0]; if (!item) return null; const artwork = item.backdrop_url || item.poster_url; return <section className="relative hidden min-h-[620px] overflow-hidden md:block">{artwork ? <img alt="" src={artwork} className="absolute inset-0 h-full w-full object-cover" /> : <div className="absolute inset-0 bg-[radial-gradient(circle_at_75%_25%,#3b1630,#08090b_58%)]" />}<div className="absolute inset-0 bg-[linear-gradient(90deg,#08090bf2_5%,#08090b9e_55%,transparent),linear-gradient(0deg,#08090b,transparent_60%)]" /><div className="relative mx-auto flex min-h-[620px] max-w-[1600px] items-center px-8"><div className="max-w-xl"><p className="text-xs font-black tracking-[.25em] text-[var(--accent)]">{item.premium ? 'DROVIXA ORIGINAL' : 'FEATURED'}</p><h1 className="mt-4 text-7xl font-black tracking-[-.05em]">{item.title}</h1><p className="mt-5 line-clamp-3 text-lg leading-8 text-white/70">{item.short_description}</p><div className="mt-8 flex gap-3"><Link className="primary-button" href={`/${item.type === 'series' ? 'series' : 'movies'}/${item.slug}`}><PlayIcon size={18} /> Watch now</Link><Link className="secondary-button" href={`/${item.type === 'series' ? 'series' : 'movies'}/${item.slug}`}>More info</Link></div></div></div></section>; }
