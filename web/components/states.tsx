export function PageLoader() { return <div className="grid min-h-[60vh] place-items-center"><div className="h-10 w-10 animate-spin rounded-full border-2 border-white/20 border-t-[var(--accent)]" /></div>; }
export function ErrorState({ retry }: { retry?: () => void }) { return <div className="mx-auto grid min-h-[55vh] max-w-lg place-items-center px-6 text-center"><div><p className="text-5xl font-black text-[var(--accent)]">!</p><h2 className="mt-4 text-2xl font-black">Something went wrong</h2><p className="mt-2 text-[var(--muted)]">Check your connection and try again.</p>{retry && <button className="primary-button mt-6" onClick={retry}>Try again</button>}</div></div>; }
export function EmptyState({ title, body }: { title: string; body: string }) { return <div className="rounded-3xl bg-[var(--card)] p-10 text-center"><p className="text-4xl text-[var(--accent)]">◇</p><h2 className="mt-3 text-xl font-black">{title}</h2><p className="mt-2 text-sm text-[var(--muted)]">{body}</p></div>; }
